import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    chickbawk: {
      primary: '#FD551B',
      secondary: '#FA9712',
      accent: '#233D4D',
      light: '#FDEFE3'
    }
  }
})

export default theme
